{
  "name": "bubble-tea-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build:api": "npm --prefix apps/api run build",
    "dev:api": "npm --prefix apps/api run start:dev",
    "lint:api": "npm --prefix apps/api run lint",
    "build:public": "npm --prefix apps/public-web run build",
    "build:admin": "npm --prefix apps/admin-web run build"
  }
}


